{
  "name": "node-auto-changelog",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "auto-changelog": "^2.2.0"
  },
  "scripts": {
    "version": "node_modules/.bin/auto-changelog -p --template keepachangelog && git add CHANGELOG.md"
  }
}
